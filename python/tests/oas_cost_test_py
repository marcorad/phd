import matplotlib.pyplot as plt
import sys

sys.path.append("../python")

import phd.scattering.oas as oas

M = [i for i in range(1, 1000)]
opt_n = [oas._optimise_cost(m) for m in M]

print(oas._block_fft_cost(201, 1024))

plt.plot(M, opt_n)
plt.show(block=True)